<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="bg-red-500 px-3 py-2 text-white p-10 rounded-md"
          >Logout</a
        >
      </div>
      <div class="flex item-start gap-3">
        <div class="w-12 h-12 rounded-md overflow-hidden"> 
          <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt=""> </div>
        <h3 class="text-3xl mb-5">
          <span class="text-yellow-500">Hello</span>, <%= user.name %>
        </h3>
      </div>
      <h5 class="mb-5 mt-3">You can create a new post</h5>
      <form action="/post" method="post">
        <textarea
          class="resize-none bg-transparent block border-2 border-zinc-800 rounded-md w-1/2"
          placeholder="what's on your mind"
          id=""
          name="content"
        ></textarea>
        <input
          type="submit"
          class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40 text-sm"
          value="Create new post"
        />
      </form>

      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach(function(post){ %>

          <div
            class="post w-1/3 p-4 mb-4 border-[1px] rounded-md border-zinc-700 bg-zinc-800"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <small class="mt-2 inline-block">
              <%= post.likes.length %> likes</small
            >
            <div class="bnts flex gap-4 mt-1">
              <a href="/like/<%= post._id %>" class="text-blue-500">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a>
              <a href="/edit/<%= post._id %>" class="text-zinc-600">edit</a>
            </div>
          </div>

          <% }); %>
        </div>
      </div>
    </div>
  </body>
</html>
